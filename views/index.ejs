<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1>Mind Page</h1>
    </header>

    <div class="post-container">
        <% for(post of posts) { %>
            <div class="post-card">
                <h3 class="user">@ <%= post.username %></h3>
                <p class="content"><%= post.post %></p>
                <div class="post-links">
                    <a href="http://localhost:3000/posts/<%= post.id %>" class="post-detail-link">See in detail</a>
                    <a href="http://localhost:3000/posts/<%= post.id %>/edit" class="post-edit-link">Edit</a>
                </div>
                <form action="http://localhost:3000/posts/<%= post.id %>?_method=DELETE" method="POST" class="delete-form">
                    <button class="delete-btn">Delete Post</button>
                </form>
            </div>
        <% } %> 
    </div>

    <a href="http://localhost:3000/posts/new" class="create-btn">Create New Post</a>

    <%- include("footer.ejs") %>
</body>
</html>
