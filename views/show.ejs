<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Post Details</title>
    <link rel="stylesheet" href="/style.css"> <!-- Corrected path -->
</head>
<body>
    <div class="post-details-container">
        <h2 class="page-title">Post Details</h2>
        <a href="/posts" class="back-btn">Back to Home</a>
        <% if (post) { %>
            <div class="post">
                <p class="post-id">Post ID: <%= post.id %></p>
                <h3 class="user">@<%= post.username %></h3>
                <h4 class="content"><%= post.post %></h4>
            </div>
        <% } else { %>
            <h3 class="no-post">Post not found!</h3>
        <% } %>
    </div>
</body>
</html>
